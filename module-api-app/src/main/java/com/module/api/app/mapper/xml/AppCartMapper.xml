<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.module.api.app.mapper.AppCartMapper">

    <insert id="newAddToCart" parameterType="com.module.api.app.entity.AppCart">
        insert into app_cart (product_id,sku_id,number,user_id) VALUES (#{productId},#{skuId},#{number},#{userId})
    </insert>

    <select id="getOldCartByParam" parameterType="java.lang.Integer" resultType="com.module.api.app.entity.AppCart">
        select id,product_id,sku_id,user_id,number from app_cart where product_id=#{productId} and sku_id=#{skuId} and user_id=#{userId}
    </select>

    <update id="updateCart" parameterType="com.module.api.app.entity.AppCart">
        update app_cart
        <set>
            <if test="appCart.number!=null and appCart.number!=''">
                app_cart.number=#{appCart.number}
            </if>
        </set>
        <where>
            app_cart.id=#{appCart.id}
        </where>
    </update>

    <select id="appCartList" resultType="com.module.api.app.result.AppCartResult">
       select app_cart.product_id,sku_id,number,main_img,name,sku,marked_price,
       CAST(app_product_sku.marked_price * number AS DECIMAL(10,0)) total_price from
        (app_cart left join app_product on app_cart.product_id=app_product.id)
       left join app_product_sku on app_product_sku.id=app_cart.sku_id
       where user_id=#{userId}
    </select>

    <delete id="delCartBySkuIds" parameterType="int">
        DELETE FROM  app_cart  WHERE user_id=#{userId} and sku_id IN
        <foreach collection="array" item="skuId" open="(" separator="," close=")">
            #{skuId}
        </foreach>
    </delete>
</mapper>